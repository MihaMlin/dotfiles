# Initialize NVM
#
# https://github.com/nvm-sh/nvm?tab=readme-ov-file#installing-and-updating

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && source "$NVM_DIR/nvm.sh"                  # Naloži NVM
[ -s "$NVM_DIR/zsh_completion" ] && source "$NVM_DIR/zsh_completion"  # ZSH autocomplete
