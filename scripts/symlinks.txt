# Dotfiles Symlink Manifest
# Format: source => target

# -------------------------------------------------------------
# Shell
# -------------------------------------------------------------
zsh/.zshrc            => ~/.zshrc
zsh/themes/p10k.zsh   => ~/.config/zsh/p10k.zsh

# -------------------------------------------------------------
# Terminal
# -------------------------------------------------------------
config/tmux/tmux.conf => ~/.config/tmux/tmux.conf

# -------------------------------------------------------------
# Git
# -------------------------------------------------------------
config/git/config     => ~/.config/git/config
