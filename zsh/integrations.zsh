# -----------------------------
# External Integrations
# -----------------------------

# FZF integration
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

# Load P10k config
[[ -f ~/.p10k.zsh ]] && source ~/.p10k.zsh
